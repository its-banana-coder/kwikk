# Kwikk Social Video Creation Workflow

This has moved to [`.claude/skills/kwikk-video-workflow/SKILL.md`](.claude/skills/kwikk-video-workflow/SKILL.md)
so it's installable as a proper skill, not just a doc to copy-paste.

**If you're using Claude Code:** the skill is auto-discovered from this
repo — just ask it to build a reel and it picks the workflow up.

**Any other AI coding tool** (Codex, Cursor, etc.): point it at
`.claude/skills/kwikk-video-workflow/SKILL.md` as custom
instructions/system prompt for video-creation tasks. It's a plain
Markdown file with a YAML frontmatter header — safe to feed to anything
that accepts freeform instructions.

See the [README](README.md#model-context-protocol-server) for how to
connect kwikk's MCP server to your tool first.
